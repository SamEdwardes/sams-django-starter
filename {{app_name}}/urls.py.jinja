from django.urls import path

from . import views

app_name = "{{ app_name }}"

urlpatterns = [
    path("", views.index, name="index"),
    path("about/", views.about, name="about"),
    path("accounts/profile", views.account_profile, name="accounts_profile"),
]
